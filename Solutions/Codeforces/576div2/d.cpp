#include <bits/stdc++.h>
using namespace std;
const int maxn = 2*100000+10;
int num[maxn];
int main()
{
    int n;
    cin >> n;
    for(int i = 1; i <= n; i++)
        cin >> num[i];
    int q;
    cin >> q;
    while(q--)
    {
        
    }
}